# ChatSystem - Lynx 聊天室

基于 **Lynx + React + WebSocket** 实现的实时聊天室示例。

## 快速开始

### 环境要求

- Node.js ≥ 18

### 安装依赖

```bash
npm install
```

### 启动开发服务器

**重要：需要同时启动两个服务**

1. **启动 WebSocket 服务器**（必须先启动）
   ```bash
   npm run server
   ```
   看到 `[server] WebSocket chat server is running on ws://0.0.0.0:8080` 表示启动成功

2. **启动前端开发服务器**（新开一个终端窗口）
   ```bash
   npm run dev
   ```
   使用 LynxExplorer App 扫描终端输出的二维码即可在手机/模拟器中打开页面

> 💡 **提示**：如果看到"连接已关闭"提示，请检查 WebSocket 服务器是否已启动

## 项目结构

```
src/
├── components/      # UI 组件
│   ├── ChatHeader   # 聊天室头部
│   ├── UserSection  # 用户ID设置
│   ├── MessageList  # 消息列表
│   ├── MessageItem  # 单条消息
│   ├── EmptyState   # 空状态
│   └── InputBar     # 输入栏
├── hooks/          # 自定义 Hooks
│   └── useWebSocket # WebSocket 连接管理
├── types.ts        # 类型定义
├── utils.ts        # 工具函数
└── App.tsx         # 主应用组件
```

## 功能特性

- ✅ 实时消息收发（WebSocket）
- ✅ 用户身份管理
- ✅ 消息去重
- ✅ 自动滚动到最新消息
- ✅ 连接状态显示
- ✅ 消息长度限制

## 技术栈

- **框架**: [Lynx](https://lynxjs.org/zh/index.html)
- **UI**: Lynx 原生组件（page, view, text, input, scroll-view）
- **状态管理**: React Hooks
- **实时通信**: WebSocket (@lynx-js/websocket)
- **构建工具**: Rspeedy

## 开发命令

```bash
npm run dev      # 启动开发服务器
npm run build    # 构建生产版本
npm run preview  # 预览构建结果
npm run server   # 启动 WebSocket 服务器
npm test         # 运行测试
```

## 配置说明

- **前端端口**: 3000（可在 `lynx.config.ts` 中修改）
- **WebSocket 端口**: 8080（可在 `server/index.mjs` 中修改）

## 许可证

本项目采用 Apache License 2.0 许可协议。
